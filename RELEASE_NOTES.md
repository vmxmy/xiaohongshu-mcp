# 小红书 MCP 工具 - 版本说明

**编译时间**: 2026-01-21 04:58
**版本**: v1.0.0
**平台**: macOS ARM64 (Apple Silicon)

## 📦 本次更新内容

### 新增功能（共22个MCP工具）

#### 🆕 关注/取关功能
- **工具 14**: `follow_user` - 关注/取关用户
  - 智能状态检测，避免重复操作
  - 支持通过 userID 定位用户

#### 🆕 评论点赞功能
- **工具 15**: `like_comment` - 点赞/取消点赞评论
  - 支持通过 commentID 或 userID 定位评论
  - 自动检测点赞状态

#### 🆕 分享功能
- **工具 16**: `share_feed` - 分享笔记获取链接
  - 自动点击分享按钮
  - 获取分享链接

#### 🆕 删除功能
- **工具 17**: `delete_feed` - 删除自己的笔记
- **工具 18**: `delete_comment` - 删除自己的评论
  - 需要确认操作，防止误删

#### 🆕 数据获取功能
- **工具 19**: `get_my_stats` - 获取个人统计数据
  - 粉丝数、关注数、获赞数、笔记数等
- **工具 20**: `get_followers_list` - 获取粉丝列表
  - 支持限制返回数量（默认20，最大100）
- **工具 21**: `get_following_list` - 获取关注列表
  - 支持限制返回数量（默认20，最大100）
- **工具 22**: `get_my_feeds` - 获取自己的笔记列表
  - 支持限制返回数量（默认20，最大100）

### 🐛 Bug修复
- 修复了 YAML 配置文件的 `attributes` 字段格式错误
- 移除了重复的 `makeUserProfileURL` 函数定义
- 归档了分析脚本，避免 main 函数冲突

### 🔧 优化改进
- 智能选择器系统支持热更新（每5秒检查配置文件）
- 添加了关注按钮的选择器配置
- 所有代码已格式化并通过编译测试

## 📋 完整工具列表（22个）

### 基础功能（1-4）
1. `check_login_status` - 检查登录状态
2. `get_login_qrcode` - 获取登录二维码
3. `delete_cookies` - 删除cookies重置登录
4. `publish_content` - 发布图文内容

### 内容浏览（5-8）
5. `list_feeds` - 获取首页Feeds列表
6. `search_feeds` - 搜索内容
7. `get_feed_detail` - 获取笔记详情
8. `user_profile` - 获取用户主页

### 互动功能（9-15）
9. `post_comment_to_feed` - 发表评论
10. `reply_comment_in_feed` - 回复评论
11. `publish_with_video` - 发布视频内容
12. `like_feed` - 点赞/取消点赞笔记
13. `favorite_feed` - 收藏/取消收藏笔记
14. `follow_user` - 关注/取关用户 ⭐新增
15. `like_comment` - 点赞/取消点赞评论 ⭐新增

### 内容管理（16-18）
16. `share_feed` - 分享笔记获取链接 ⭐新增
17. `delete_feed` - 删除自己的笔记 ⭐新增
18. `delete_comment` - 删除自己的评论 ⭐新增

### 数据获取（19-22）
19. `get_my_stats` - 获取个人统计数据 ⭐新增
20. `get_followers_list` - 获取粉丝列表 ⭐新增
21. `get_following_list` - 获取关注列表 ⭐新增
22. `get_my_feeds` - 获取自己的笔记列表 ⭐新增

## 🚀 使用方法

### 1. 启动服务
```bash
./xiaohongshu-mcp
```

### 2. 配置 MCP 客户端
在你的 MCP 客户端配置中添加：
```json
{
  "mcpServers": {
    "xiaohongshu": {
      "command": "/Users/xumingyang/app/xiaohongshu-mcp/xiaohongshu-mcp"
    }
  }
}
```

### 3. 使用示例

#### 检查登录状态
```
使用 check_login_status 工具检查是否已登录
```

#### 搜索并点赞
```
1. 使用 search_feeds 搜索"咖啡"相关内容
2. 使用 like_feed 点赞感兴趣的笔记
```

#### 获取个人数据
```
使用 get_my_stats 查看我的粉丝数、获赞数等统计信息
```

#### 管理关注
```
1. 使用 get_following_list 查看我关注的人
2. 使用 unfollow_user 取关不活跃的用户
```

## ⚠️ 注意事项

### 操作频率限制
为避免账号被封，建议遵循以下频率限制：

- **点赞**: 每小时不超过60次，每次间隔至少30秒
- **评论**: 每小时不超过20次，每次间隔至少2分钟
- **关注**: 每小时不超过30次，每次间隔至少1分钟
- **发布**: 每天不超过5条，每次间隔至少2小时

### 安全建议
1. 不要使用批量操作，让AI控制调用频率
2. 操作间隔加入随机延迟，模拟人类行为
3. 遇到错误立即停止，检查账号状态
4. 定期检查 `configs/selectors.yaml` 配置是否需要更新

## 🔧 配置文件

### 智能选择器配置
配置文件位置: `configs/selectors.yaml`

该文件支持热更新（每5秒自动检查），无需重启服务。

包含以下元素的选择器：
- 点赞按钮
- 收藏按钮
- 评论输入框
- 提交按钮
- 回复按钮
- 关注按钮

## 📝 已知问题

1. 某些笔记可能不支持评论（网页端限制）
2. 分享链接获取依赖剪贴板权限
3. 删除操作需要确认，可能需要额外等待时间

## 🎯 下一步计划

根据用户需求，可以考虑添加：
- 私信功能（发送/接收私信）
- 获取点赞/收藏的笔记列表
- 拉黑/取消拉黑用户
- 更多数据统计功能

## 📞 反馈

如有问题或建议，请通过以下方式反馈：
- 查看日志输出了解详细错误信息
- 检查 `configs/selectors.yaml` 配置是否正确
- 确认小红书页面结构是否发生变化

---

**编译信息**:
- Go版本: 使用项目 go.mod 中指定的版本
- 平台: darwin/arm64
- 文件大小: 21MB
- 可执行文件: `xiaohongshu-mcp`
