# 手动测试小红书发布功能 - 观察真实返回信息

## 准备工作

1. ✅ 浏览器已打开: https://creator.xiaohongshu.com/publish/publish
2. ✅ 已登录小红书账号
3. ⬜ 打开浏览器开发者工具（F12 或 右键->检查）
4. ⬜ 切换到 **Network（网络）** 面板
5. ⬜ 勾选 **Preserve log（保留日志）**

## 测试步骤

### 步骤 1: 上传图片
- 点击页面上的"上传图片"按钮
- 选择测试图片: `/Users/xumingyang/app/xiaohongshu-mcp/test_images/test.jpg`
- 等待图片上传完成

### 步骤 2: 填写内容
- 标题: `测试发帖-手动测试`
- 内容: `这是一条测试笔记，用于观察发布API的真实返回信息`
- 标签: 不填或填 `#测试`

### 步骤 3: 观察网络请求（重点！）
**在点击发布前:**
1. 确保 Network 面板已打开
2. 点击 Network 面板的"清除"按钮（🚫图标）
3. 准备好观察

**点击发布按钮后，立即观察:**

#### 查找关键请求
在 Network 面板中查找包含以下关键词的请求:
- `note`
- `publish`
- `web_api/sns`
- `v1` 或 `v2`

#### 记录信息
对于找到的关键请求，记录:

1. **请求 URL**:
   - 完整的 URL 是什么？
   - 例如: `https://creator.xiaohongshu.com/web_api/sns/v2/note`

2. **请求方法**:
   - GET / POST / PUT ?

3. **状态码**:
   - 200 / 400 / 500 ?

4. **响应内容**:
   - 点击该请求
   - 切换到 "Response（响应）" 标签页
   - 复制完整的 JSON 响应内容

5. **请求载荷**:
   - 切换到 "Payload（载荷）" 标签页
   - 看看发送了什么数据

### 步骤 4: 观察页面反馈
- 是否有成功提示弹窗？
- 是否有错误提示？
- 页面是否跳转？
- 跳转到哪个 URL？

### 步骤 5: 验证结果
- 在小红书创作中心的"笔记管理"中是否能看到这条笔记？
- 笔记状态是什么？（已发布/审核中/草稿）

## 期望观察到的信息

### 成功的情况
```json
{
  "success": true,
  "code": 0,
  "msg": "成功",
  "data": {
    "note_id": "xxxxx",
    "status": "published" 或 "审核中"
  }
}
```

### 失败的情况
```json
{
  "success": false,
  "code": 错误代码,
  "msg": "具体错误信息"
}
```

---

## 完成后
请将观察到的信息告诉我，特别是:
1. 关键 API 的完整 URL
2. 响应的 JSON 内容
3. 页面上显示的提示信息
